apiVersion: v2
description: A Helm Chart for cortex-tenant
name: cortex-tenant
version: 0.8.1 # This is the chart version
appVersion: v2.0.0 # version number of the application being deployed.
type: application
sources:
  - https://github.com/blind-oracle/cortex-tenant
